# Normalize text line endings in the repo, check out native on Windows.
* text=auto

# Never run line-ending conversion on binaries — doing so corrupts them.
*.pdf  binary
*.jpg  binary
*.jpeg binary
*.png  binary
*.webp binary
*.ico  binary
*.woff  binary
*.woff2 binary
